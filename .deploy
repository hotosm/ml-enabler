{
    "profile": "devseed",
    "artifacts": {
        "docker": [
            "{{project}}:{{gitsha}}",
            "{{project}}:task-{{gitsha}}"
        ],
        "lambda": [
            "devseed-artifacts/ml-enabler/lambda-{{gitsha}}.zip"
        ]
    }
}
