{
    "profile": "devseed",
    "artifacts": {
        "docker": [
            "{{project}}:{{gitsha}}",
            "{{project}}:task-build-{{gitsha}}",
            "{{project}}:task-retrain-{{gitsha}}"
        ],
        "lambda": [
            "devseed-artifacts/ml-enabler/lambda-{{gitsha}}.zip"
        ]
    }
}
